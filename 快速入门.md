# 🚀 Ralph 嵌入式 Web 服务器 - 快速入门

## 5 分钟快速开始

### 第一步：构建

```bash
cargo build -p ralph-cli --features embedded-web --release
```

等待构建完成（约 1-2 分钟）

### 第二步：运行

```bash
./target/release/ralph web
```

服务器会自动启动，浏览器会自动打开 http://localhost:3000

### 第三步：使用

在 Web 界面中：
1. 创建任务
2. 查看任务列表
3. 运行任务
4. 查看日志

## 📋 常用命令

```bash
# 启动服务器（默认端口 3000）
./ralph web

# 指定端口
./ralph web --backend-port 8080

# 不打开浏览器
./ralph web --no-open

# 指定项目目录
./ralph web --workspace /path/to/project

# 查看帮助
./ralph web --help
```

## 🔌 API 快速测试

```bash
# 健康检查
curl http://localhost:3000/api/v1/health

# 创建任务
curl -X POST http://localhost:3000/api/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{"id":"my-task","title":"我的任务","priority":2}'

# 列出任务
curl http://localhost:3000/api/v1/tasks

# 获取任务详情
curl http://localhost:3000/api/v1/tasks/my-task

# 运行任务
curl -X POST http://localhost:3000/api/v1/tasks/my-task/run
```

## 📦 文件大小

```
20MB - 包含所有 Web 前后端功能
```

## ✨ 特性

- ✅ **零依赖** - 无需 npm install
- ✅ **单文件** - 一个二进制包含所有内容
- ✅ **开箱即用** - 直接运行
- ✅ **跨平台** - macOS、Linux、Windows

## 📚 更多文档

- **完整使用指南**: `嵌入式Web服务器使用指南.md`
- **实现细节**: `EMBEDDED_WEB_IMPLEMENTATION.md`
- **项目计划**: `plan0.md`

## 🎉 开始使用

```bash
# 1. 构建
cargo build -p ralph-cli --features embedded-web --release

# 2. 运行
./target/release/ralph web

# 3. 打开浏览器访问
# http://localhost:3000
```

就这么简单！🎊
